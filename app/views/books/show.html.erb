<p><%= flash[:notice] %></p>
<table>
  <tr>
    <th>Title:</th>
    <td><%= @book.title %></td>
  </tr>

  <tr>
    <th>Body:</th>
    <td><%= @book.body %></td>
  </tr>
</table>

<br>

<%= link_to 'edit', edit_book_path(@book.id) %>
<%= link_to 'back', "/books" %>